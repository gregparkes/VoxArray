CC=g++
DEPS=types.h VarStructs.h numpy.h Vector.h Matrix.h
OBJ1=numstatic.o numpy.o Vector.o Matrix.o test_matrix.o main.o

%.o: %.c $(DEPS)
	$(CC) -c -o $@ $< -I.

test_numpy: $(OBJ1)
	$(CC) -o test_numpy $^ -I.
